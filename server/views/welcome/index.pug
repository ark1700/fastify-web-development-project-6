extends ../layouts/application.pug

block content
  .card
    .card-body.p-5.bg-light
      h1.display-4.mb-4= t('views.welcome.index.hello')
      p.lead.mb-4= t('views.welcome.index.description')
      hr
      .d-flex.gap-3
        a.btn.btn-primary(href=route('newSession'))= t('layouts.application.signIn')
        a.btn.btn-primary(href=route('newUser'))= t('layouts.application.signUp')
